{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Process Payment",
  "description": "AP Specialist confirms payment entry in Oracle and records the payment reference",
  "type": "object",
  "required": ["payment_reference", "payment_method_used", "payment_successful"],
  "properties": {
    "payment_reference": {
      "type": "string",
      "title": "Oracle Payment Reference Number",
      "description": "The payment batch or reference number generated by Oracle"
    },
    "payment_method_used": {
      "type": "string",
      "title": "Payment Method Used",
      "enum": ["ach", "check"],
      "enumNames": ["ACH Direct Deposit", "Paper Check"]
    },
    "payment_successful": {
      "type": "boolean",
      "title": "Payment Processed Successfully?",
      "description": "Confirm Oracle accepted the payment without errors"
    },
    "payment_failure_reason": {
      "type": "string",
      "title": "Failure Reason",
      "description": "If payment failed, briefly describe the error from Oracle"
    }
  }
}
